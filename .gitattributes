# Convert all text files to LF (Unix-style line endings)
* text=auto eol=lf

# Ensure specific file types always use LF endings
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf
*.py text eol=lf
*.sh text eol=lf

# Optional: If you want to allow CRLF for certain files (e.g., Windows-specific scripts), you can define those separately.
*.bat text eol=crlf
